{
  "name": "zihinturu",
  "private": true,
  "version": "0.1.0",
  "description": "Zihin Turu — Türkçe zihin oyunu platformu",
  "workspaces": ["paketler/*", "uygulama"],
  "scripts": {
    "gelistir": "npm run dev --workspace=uygulama",
    "insa": "npm run build --workspace=uygulama",
    "onizle": "npm run preview --workspace=uygulama",
    "test": "vitest run",
    "test:izle": "vitest",
    "tip": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
